# CLAUDE.md — SafeHands (Pharos)

Guidance + resume-context for Claude working in this repo. **Read this first.**

## What SafeHands is
A **pre-execution security & policy layer for autonomous agent finance on Pharos Pacific Mainnet (chain 1672)**. Before an agent signs / approves / swaps / pays, a deterministic policy engine returns `ALLOW` / `BLOCK` / `REQUIRE_CONFIRMATION` / `PREPARE_ONLY`. Zero-custody: never holds keys, never signs.

## ⚠️ TWO separate codebases in ONE repo — do not conflate them
1. **`anvita/safehands/`** — the **Anvita Flow hosted skill** = the hackathon deliverable. A standalone, **zero-dependency** engine (`scripts/safehands-engine.js`) that reads Pharos RPC + GoPlus directly. Read-only, no backend, no keys. **Does NOT import `src/`.** Package with `npm run package:anvita` → `dist/safehands.zip`.
2. **`src/`** — the full TypeScript product: **MCP server + CLI + npm SDK + HTTP API + x402 + attestation**, compiled to `dist/`. IMPORTANT: **MCP, CLI, and SDK ARE this** (`bin` → `dist/index.js` for CLI/MCP; `main`/`types`/`exports` → `dist/sdk.js` for the SDK barrel — see re-audit note). **Deleting `src/` kills all three.**

## 🎯 Anvita Agent Carnival — Phase 2 hosting rule (authoritative, from organizers, 2026-07-07)
> "The service agent will be hosted by Anvita Flow, not your own local cloud. Developers should go to our developer platform to upload the skills, and we'll package the skills and provide agent runtime."

- Deliverable = the **skill uploaded to Anvita's dev platform**; Anvita provides the runtime.
- The agent must NOT depend on your own backend/cloud → **Railway / self-hosted backend must NOT be presented as the service agent.**
- `anvita/safehands/` already complies (zero backend; public RPC + GoPlus only).
- MCP/CLI/SDK do **not** break the rule (they're developer self-host/distribution, not the hosted agent) — keeping them is fine; just don't frame them as the Anvita agent.

## ✅ DONE — Option A executed (2026-07-07): Railway story removed, `src/` kept as self-host reference
User chose **Option A**. `src/` (MCP/CLI/SDK/HTTP API) kept as open-source reference / optional self-host; Anvita skill is the star. The Railway/own-cloud deploy story is fully removed and docs reframed to generic self-host.
- **Deleted:** `railway.json`, `Procfile`, `docs/RAILWAY_DEPLOYMENT.md`, `docs/RAILWAY_SMOKE_TESTS.md`, `docs/deployment/RAILWAY_ZERO_CUSTODY_ENV.md`, `scripts/railway-smoke-test.mjs`, and the `railway:smoke` npm script. (Backups were in the session scratchpad only — not restorable long-term since this isn't a git repo.)
- **Reframed** (no dead URL / no broken link left): README (self-host heading), `REVIEWER_QUICKSTART` + `SAFEHANDS_REVIEWER_DEMO_SCRIPT` (shipped docs → Anvita agent + `npx --demo` + `localhost:4022`), INDEX, PRODUCTION_BACKEND, REFERENCES, ANVITA_FLOW, REALFI_RWA_ALIGNMENT, SECURITY, OBSERVABILITY_AND_ACTIVITY, ACCESS_CONTROL, PREPARE_TRANSACTION; **full host-agnostic rewrite** of `docs/deployment/ARCHITECTURE_DECISION.md` + `FULL_SERVICE_DESIGN.md`. Dockerfile comments softened (Dockerfile kept). `.gitignore` internal-docs block restored.
- **Deliberately NOT changed:** `src/` code untouched. The public API field name `railwayReady` is **KEPT** (renaming would break the `/public-config` capability contract of the live API); docs keep the literal identifier. This is the only remaining "railway" token, plus generic `Railway/Fly/VPS` example-host comments in `src/`.
- **Verified green end-to-end (this session):** `tsc` build exit 0; MCP stdio handshake → **33 tools** (8/8 write + all read, none missing); CLI `--help`/`skill` list; READ preflight → `BLOCK`; WRITE gated (`WRITE_TOOLS_DISABLED`; with `WRITE_TOOLS_ENABLED=true` → advances to `NO_SIGNER_AVAILABLE`, pipeline intact); HTTP API `/health` + `/public-config` (chain 1672) + gated write route; `npm pack --dry-run` + `package:anvita` (`dist/safehands.zip`, `unzip -t`) OK. **SDK exports (RESOLVED on `harden/production-readiness`):** `src/sdk.ts` is now a real named-export barrel and `main`/`types`/`exports` point to `dist/sdk.js`, so `import { evaluateActionPolicy } from 'safehands-pharos'` resolves to callable, side-effect-free exports (does NOT start the MCP server or read keys). `bin` still → `dist/index.js` (CLI/MCP).

## ✅ Already done — audit Phase 2 (do NOT redo)
A full strict audit + minimal fixes were applied and **verified green**:
- Fixed the fresh-clone build (was broken: `src/api/server.ts` imported missing `./demoPage.js`; real file is `consolePage.ts`).
- Added secret-free `.env.example`; repointed `package:anvita` to the lean packager (`scripts/package-anvita-safehands.mjs`) and deleted the misleading full-backend `package-anvita.mjs`; fixed `package.json` `files[]`; renamed `.agents/policies/a.json` → `default.json`; corrected README/quickstart overclaims (removed unverifiable "19 offline tests" claim; honest `npm test` = live-RPC wording; roadmap → v2.3.0); fixed `Dockerfile` (`--ignore-scripts` on both `npm ci`, `EXPOSE` 4022).
- Verified: `npm ci` / build / `npm test` (12/12 at the time — suite has since grown to **68**; see re-audit) / `npm run demo` / `package:anvita` / `unzip -t` / `npm pack --dry-run` / `docker build`+run(`/health`) / compiled CLI — all pass. Prod-dep `npm audit` = 0 vulns (the 2 high are dev-only Hardhat).
- Clean GitHub-publish zip already built at `dist/safehands-pharos-github.zip` (excludes internal docs + build artifacts + secrets).

## 🔒 Internal docs — NEVER publish to public GitHub
`docs/pitch/` (all), `docs/deployment/DESIGN_DECISION_REGISTER.md`, `docs/deployment/PHASE_D_LIVE_MAINNET_RUNBOOK.md`, `docs/deployment/NPM_PUBLISH_CHECKLIST.md`.
✅ The `.gitignore` "Internal docs" block is now **restored** (2026-07-07) — these stay out of any `git push`. Rule: publish only external-audience docs (reviewers/judges/users); keep pitch strategy, operator runbooks, and work-tracking local.

## Build / verify
`npm ci` (runs build via `prepare`) · `npm run build` · `npm test` (**76 tests / 22 suites, hermetic-offline**; live RPC isolated to `npm run test:live`, never broadcasts) · `npm run demo` (12 non-destructive scenarios) · `npm run package:anvita` → `dist/safehands.zip` · `docker build` (optional self-host backend image; **default port 4022**, set `PORT` for your host).

## Guardrails
- **Never invent or alter on-chain addresses** — Registry `0x428e02bf…8b06c8d`, Attestation `0x71a7a87b…5b4588c`, chain `1672`. Verify on Pharosscan; do not fabricate.
- **No secrets in the repo** — `.env.example` = placeholders only.
- Keep changes **minimal & submission-oriented**; don't redesign or add major features (LP manager, paymaster, new contracts/chains, wallet infra) without explicit user approval.
- **Keep the build green:** `tsconfig` has `noEmitOnError`, so a single missing-module import (e.g. a renamed file whose import wasn't updated) breaks `npm ci` everywhere. Watch for this.

## Key facts / state
- Railway/own-cloud deploy story removed (Option A, 2026-07-07). We present **no hosted HTTP API of our own** — the hosted agent is the Anvita skill; `src/`'s HTTP API is optional self-host only. (An external Railway instance may still be running at v2.2.0; it's no longer part of the repo story — tear it down when convenient.)
- Anvita agent (per prior notes): "SafeHands Agent" `agent_7UCLA5MLTGNE` — **NOT live yet** (confirmed 2026-07-07). Verify it's actually live/registered before any "live on Anvita" claim.
- **Pitch deck (`docs/pitch/SAFEHANDS_PITCH_DECK.html`) still carries the old Railway URL/story — intentionally deferred** until the Anvita agent is live (can't yet make an honest "live on Anvita" claim; don't swap a dead URL for an unverified one). Update it to Anvita-first + local demo (`npx --demo` / `localhost:4022`) once the agent is registered. Internal DDR + Phase-D runbook were reconciled 2026-07-07 (Option A banner + host-agnostic).
- Contracts (`SafeHandsRegistry.sol`, `SafeHandsAttestation.sol`) are sound: `Ownable2Step`, renounce disabled, double-hashed Merkle leaves, immutable privacy-preserving attestation.

## 🔍 Production-readiness re-audit — branch `harden/production-readiness` (2026-07-07, session 2)
Deep audit of the uncommitted hardening branch (3 parallel security/build/API read-only agents + live verification). **Full remediation plan:** `~/.claude/plans/validated-baking-dewdrop.md`.

**── ✅ Session 3 (2026-07-08) — P0-1 FIXED + quick wins (via `/product-review` remediation) ──**
- **P0-1 (GoPlus schema-drift fail-open) → FIXED on BOTH surfaces.** `src/tools/checkTokenSecurity.ts` now parses GoPlus via an exported pure fn `interpretGoplusTokenResult` that returns `null` → fail-closed `TOKEN_SECURITY_DATA_UNAVAILABLE` when the honeypot field is absent/retyped, and is type-tolerant (numeric `1` honeypots now caught, not just `"1"`). The Anvita engine `goplusToken`/`analyzeContract` mirror it: a `schemaDrift` result (present-but-unreadable) now floors to `warn` like an outage. New hermetic tests: `test/token-security-failclosed.test.ts` (7 cases) + 1 anvita drift case.
- **Quick wins:** demo scenario-2 label `ALLOW`→`PREPARE_ONLY` (`src/demo.ts`); README "🏆 NEW…fully-hosted" header → honest "launching / once published" + new "Which SafeHands is for me?" surface table + verdict-vocabulary cross-map; `@openzeppelin/contracts` → **devDependencies** (only `contracts/*.sol` uses it, never `src/` runtime); `engines.node` `>=18`→`>=20` (dev 20 / CI 22).
- **Verified green:** `npm run build` (tsc, `noEmitOnError`) exit 0 · `npm test` **76/76 (22 suites), 0 fail** · compiled `dist/` carries the fix · Anvita engine still live (chain 1672).
- **Still open (deferred, write-mode only):** P0-2 (honeypot→confirmable on GoPlus outage) + P0-3 (unpriceable-token swap uncapped) — only bite with `WRITE_TOOLS_ENABLED` + a funded key. **Read-only launch (Anvita skill + npm) now has 0 P0 blockers AND 0 recommended.** Remaining P2: no lint step (Node-drift + openzeppelin mis-categorization now resolved). **UPDATE session 4: P0-2/P0-3 no longer deferred — user chose FIX in plan-Phase-2 (see Session 4).**

**── ✅ Session 4 (2026-07-08) — PHASE2_HARDENING_PLAN Phase 1 EXECUTED ──**
Plan doc: `docs/pitch/PHASE2_HARDENING_PLAN.md` (internal, gitignored). User approved a reordered sequence: **Langkah 0 decision = H-1/H-2 (P0-2/P0-3) WILL BE FIXED in plan-Phase-2** (not deferred) · B-4 pulled forward and tested in parallel · plan-Phase-3a (package+upload Anvita) starts right after Phase 1; plan-Phase-2 runs in the Anvita-verification wait window · Phase 3b (flip "once published"→live claims) only after the agent is verified live.
- **Phase 1 applied (all uncommitted):** B-1 stale counts removed (`docs/REVIEWER_QUICKSTART.md:27` "68 checks"→drift-proof scope wording; `docs/SAFEHANDS_REVIEWER_DEMO_SCRIPT.md:100` "12 smoke tests"→suite wording) · B-2 "experimental & unaudited, ship disabled, opt-in self-hosted single-tenant" labels ×4 (README "What it is — and is not" + "Execution — gated" table intro, SKILL.md capability 5, REVIEWER_QUICKSTART gated-execution) · **H-3 (only behavior change): Anvita engine now floors reachable-but-unindexed non-canonical tokens** (`gpUnindexedToken` in `analyzeContract`; fresh-rug window can no longer score `allow`; canonical-registry tokens exempt) + new hermetic test case · H-4 write-gate header comment now states the honeypot-hard-BLOCK is GoPlus-contingent (P0-2 tracked).
- **Verified green:** build exit 0 · `npm test` **77/77 (22 suites)** · `package:anvita` + `unzip -t` OK and the zip carries the H-3 fix · claim-sweep greps clean (stale counts: 0 hits; "live on Anvita": 0 hits; railway: only deliberate src/ host-example comments + `railwayReady`).
- **B-4 tested for real and FAILED (as the plan predicted):** GitHub `SZtch/safehands-pharos` is public BUT default branch = **`mainnet-railway`, last pushed 2026-07-03 (pre-hardening, Railway-era)** and that branch has **no `prepare` script** → git-dep install never builds `dist/` → `npx -y github:SZtch/safehands-pharos --demo` fails today with `safehands-pharos: not found`. Local repo has **no git remote configured**. **Remedy (user action, before submission): push the final code to `main`, flip the GitHub default branch to `main`, then retest npx with a fresh npm cache.** Current branch has `prepare: npm run build` (package.json:58), so the pushed code will build on git-install.
- **Next per approved order:** plan-Phase-3a (package + upload to Anvita dev platform + verify `agent_7UCLA5MLTGNE` live) → plan-Phase-2 (H-1/H-2 fail-closed fixes + H-5 honesty pass + M-1/M-2) in the wait window → Phase 3b → Phase 4 polish → Phase 5 gate.

**New files this branch:** `src/lib/policy/writeExecutionGate.ts` — enforces the FULL deterministic decision at the direct write-tool boundary (previously the raw write tools halted only on `BLOCK` and **silently signed** on `REQUIRE_CONFIRMATION`/`REQUIRE_TOKEN_REVIEW`/`WARN`; now every non-ALLOW tier stops unless the caller passes explicit `confirm`). `src/sdk.ts` — the SDK barrel (above). New tests: `execution-hardening`, `sdk-exports`, `wallet-crypto`.

**Verified green (ground truth this session):** `tsc --noEmit` 0 · `npm test` **68 tests / 21 suites, 0 fail** · `npm audit --omit=dev` **0 vuln** · no hardcoded secrets · wallet crypto = **scrypt + AES-256-GCM** (random salt/IV per record, legacy XOR rejected) · **every** fund-moving tool calls `enforceWriteDecision` before signing · HTTP write routes require a **valid API key** (fail-closed) · strong SSRF defense · **33 tools** · on-chain addresses consistent everywhere.

**Verdict: mechanically production-ready; NOT yet *fully* production-ready as a security product.** Was blocked by 3 open P0 degradation/fail-open paths; **P0-1 FIXED 2026-07-08 (see Session 3 above)** — 2 remain (P0-2/P0-3, write-mode only):
- **P0-1 · GoPlus schema-drift fails OPEN** — ✅ **FIXED 2026-07-08** (pure `interpretGoplusTokenResult` fail-closed in `src/tools/checkTokenSecurity.ts` + `schemaDrift` floor in the Anvita engine; type-tolerant honeypot; +8 tests). Was: renamed/retyped GoPlus fields → all checks default "safe" → `safetyScore=100` → `ALLOW`.
- **P0-2 · Honeypot → *confirmable*, not BLOCK, when GoPlus is down / token unindexed** (`checkTokenSecurity.ts:142` → `src/lib/policy/actionPolicyEngine.ts:342` → `writeExecutionGate.ts:65`). NOTE: GoPlus is **keyless + public** (no API key needed); the app still runs when GoPlus is down — it degrades to `review` (confirmable), which is exactly the risk. → fail-closed to BLOCK on the execute/approve path.
- **P0-3 · Unpriceable-token swap escapes BOTH caps** (`src/lib/spendAccumulator.ts:62` — `estimateUsd`=0 for non-PROS/USDC/USDT; PROS ceiling only when `tokenIn==="PROS"`). → deny-by-default + hard notional cap.

**Open P1 (honesty/hygiene):** reword security claims (soft-tier `confirm` is caller-attested — trust anchor is the MCP host/human, not the guarded agent; honeypot-block is GoPlus-contingent) in `SECURITY.md`; ✅ **"live on Anvita" DONE** — softened all present-tense "lives/runs/hosted on Anvita" claims across 10 public docs to "once published" (agent `agent_7UCLA5MLTGNE` still not live yet); enforce `NODE_ENV=production` (per-route error genericization keys off it — internals leak otherwise; N/A until you self-host the HTTP API); 2 design docs (`docs/deployment/ARCHITECTURE_DECISION.md`, `FULL_SERVICE_DESIGN.md`) tracked & public → decide public-or-gitignore (claims now softened, honest either way).

**Open P2 (scale/polish):** single-instance state (spend accumulator / x402 replay / rate-limit are per-process → caps bypassable across replicas; `writeFileSync` not atomic); gate vs `src/api/broadcastRoutes.ts:37` have inconsistent self-confirmation stance; no lint step. (✅ FIXED 2026-07-08: Node drift → `engines.node >=20`; `@openzeppelin/contracts` → devDependencies.)

**Production surfaces + scope (target confirmed 2026-07-07: Anvita read-only; user NOT live on Anvita yet).** THREE distinct shippable surfaces — do not conflate:
1. **Anvita hosted skill** (`anvita/safehands/`, a SEPARATE zero-dep engine `scripts/safehands-engine.js`, NOT `src/`) — read-only, no keys. **≈0 blockers.** Only P0-1's *analog* in the Anvita engine matters (GoPlus 200-with-renamed-fields under-reports → floor to `warn`); P0-2 is already handled there (`safehands-engine.js:55-57,256` floor to `warn`/unsafe-until-confirmed on GoPlus outage); P0-3 + all HTTP/wallet items are N/A.
2. **npm package: MCP + CLI + SDK** (`src/`, via `bin`→`dist/index.js`, `main`/`exports`→`dist/sdk.js`) — **read-only by default**; write tools gated behind `WRITE_TOOLS_ENABLED` + signer. Ready read-only. **BUT** `src/sdk.ts` exports `evaluateTokenSecurityGate`, so **P0-1 (GoPlus schema fail-open) leaks into the SDK's verdict correctness — not just the write path** → P0-1 is *recommended even for a read-only npm publish*. P0-2/P0-3 only bite in write mode (`WRITE_TOOLS_ENABLED=true` + funded key) → legitimately deferred **IF** write tools ship disabled + labeled experimental/unaudited.
3. **Self-host HTTP API on mainnet with a funded wallet** — all P0 + P1 are ship-blockers. **FROZEN** — no clear Anvita write path yet (user, 2026-07-07).

**Net remaining before a read-only launch (Anvita skill + npm read-only):** 🔴 **0 blockers** · 🟢 **P0-1 fail-closed GoPlus DONE 2026-07-08** (both `src/tools/checkTokenSecurity.ts` AND `anvita/safehands/scripts/safehands-engine.js`) · 🟡 label write tools "experimental/unaudited" in README/SKILL · 🧊 P0-2/P0-3 + remaining P2 deferred (write-mode only). **Read-only launch is now unblocked with nothing recommended outstanding.**

**Doc-honesty pass DONE (2026-07-07):** softened present-tense "agent lives/runs/hosted on Anvita" → "once published/will" across **10 public docs** (README ×2, REVIEWER_QUICKSTART ×2 incl. `56 checks`→**68**, SAFEHANDS_REVIEWER_DEMO_SCRIPT, REALFI_RWA_ALIGNMENT ×2, INDEX, ANVITA_FLOW, PRODUCTION_BACKEND, REFERENCES, deployment/ARCHITECTURE_DECISION, deployment/FULL_SERVICE_DESIGN). Contracts/demo "Live" markers KEPT (true — deployed on chain 1672). README:17's "Once published" was already honest.
